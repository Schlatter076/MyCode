#include <stdio.h>
/* void sum_diff(int sum, int diff, int na, int nb)
{
	sum = na + nb;
	diff = na > nb ? na - nb : nb - na;
} */
int main(void)
{
	int i=0;
	while(i++ < 1000)
	{
		printf("\tfuck");
	}
}